/**
 * Revo Credits — shared pricing constants.
 * Never hardcode these values in services.
 * Import from here everywhere credits are calculated.
 */

export const CREDIT_COST: Record<string, number> = {
  BASIC: 5,
  PRO: 10,
};
